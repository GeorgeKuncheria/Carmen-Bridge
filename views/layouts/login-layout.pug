//- we wont add the JS files in the head section, bcs it opne start loading content until the js files are loaded
//- so its better to add them after the body tag.(enhances performance)

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Login

        //- adding bootstrap for CSS
        link(rel='stylesheet', href='https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css', integrity='sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh', crossorigin='anonymous')
        //- reference to its respective CSS
        //- the reason why we didn't give the adress as "/public/css/login.css" is because we will serve this file
        //- serving means providing static files to the user, which is not processes by the server, ex: images,CSS files,client side js files
        //- we've mention in 'app.js' to to serve files inside the public folder as static files
        link(rel="stylesheet", href="/css/login.css")
    body
        //- in pug syntax div.wrapper is same as .wrapper or anything wrt to div
        .wrapper
            block content
        //- full version of jQuery
        script(src='https://code.jquery.com/jquery-3.5.1.min.js', integrity='sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=', crossorigin='anonymous')
        //- other sricpts from the bootstrap site
        script(src='https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js', integrity='sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo', crossorigin='anonymous')
        script(src='https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js', integrity='sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6', crossorigin='anonymous')

